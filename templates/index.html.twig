{% extends 'base.html.twig' %}

{% block title %}Document Root{% endblock %}

{% block body %}
    <div class="container">
        <h1>Bienvenu !</h1>
        <div class="row">
            <div class="col-9">
                <div class="row">
                    {% for favorite in favorites %}
                        <div class="col-6">
                            <div class="card border shadow lift mb-3">
                                <div class="card-body">
                                    <div class="card-title h4 font-weight-light text-dark">
                                        {{ favorite.url }}
                                    </div>
                                    <p class="card-text">
                                        <img src="#" title="#" alt="default-img" />
                                        {{ favorite.description }}
                                    </p>
                                    <div class="d-flex justify-content-end">
                                        <a class="btn btn-info px-4" href="{{ favorite.url }}">
                                            Consulter
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="col-3">
                <ul>
                    <li>
                        <a href="{{ path('blog') }}">Le Blog</a>
                    </li>
                    <li>
                        <a href="{{ path('about') }}">A propos</a>
                    </li>
                    <li>
                        <a href="{{ path('poke') }}">Bac à sable</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

{% endblock %}
